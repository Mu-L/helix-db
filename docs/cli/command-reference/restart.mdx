---
title: "helix restart"
sidebarTitle: "restart"
pageType: "Reference"
---

<div className="flex flex-wrap gap-2"><Badge color="gray" size="sm">Reference</Badge></div>

> For the complete documentation index optimized for AI agents, see [llms.txt](/llms.txt).

Restart an existing background local instance in place, using its current image and container settings. Restart never pulls an image or creates a replacement container. If the container has been removed, the command fails. Use [`helix start`](/cli/command-reference/start) to create a container or apply new image or configuration settings.

Default local data is in-memory and is wiped by every restart. Disk-mode instances preserve data in their local MinIO volume.

## Usage

```bash
helix restart [INSTANCE]
```

## Arguments

| Argument | Description |
|----------|-------------|
| `INSTANCE` | Local instance to restart. If omitted in a TTY with multiple instances, the CLI prompts. Defaults to `dev` when present. |

## Examples

```bash
# Restart the default 'dev' instance
helix restart dev

# Restart with interactive picker
helix restart
```
