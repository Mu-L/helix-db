//! Native terminal lowering contract tests.
//!
//! Child modules cover each terminal family and input-selection boundary
//! independently so new terminal shapes get focused tests.

mod aggregate;
mod inputs;
mod payload;
mod projection;
mod reserved;
mod support;
mod validation;
